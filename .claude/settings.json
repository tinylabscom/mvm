{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | grep -Eq '(^|[[:space:]>|&])-:-([[:space:]]|;|&|\\||$)' && printf '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Blocked: this command targets a file named -:-, which is junk from a malformed redirect. Redirect to a real filename or stdout instead.\"}}' || true"
          }
        ]
      }
    ]
  }
}
