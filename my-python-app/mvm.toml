# mvm microVM manifest — generated by `mvmctl init`.
# Edit sizing, profile, or source selector as needed.
name = "my-python-app"
flake = "."
profile = "default"
vcpus = 2
mem = "512M"
data_disk = "0"
net = false
