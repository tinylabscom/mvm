---
// The browser-tier microVM demo has moved to the WebLinux demo. Redirect the
// old /demo route so bookmarks and the landing page still land on a working
// demo instead of the legacy WASI sandbox page.
return new Response(null, {
  status: 302,
  headers: {
    "Location": "/demo/weblinux/",
  },
});
---
