# Linux 6.12.110 synchronized kernel pin

Backing: shipped-source
Validation: check-sprint-append

**Issue:** [#3248](https://github.com/tinylabscom/mvm/issues/3248)

## Outcome

Move the workload/builder kernel and libkrun firmware build from Linux 6.12.109
to the latest 6.12 LTS point release reported by the freshness watcher. Keep
both consumers pinned to one verified upstream archive.

## Checklist

- [x] Reproduce the structural pin mismatch before changing the consumers.
- [x] Verify the Linux 6.12.110 archive digest against kernel.org's signed
      checksum manifest and Greg Kroah-Hartman's detached release signature.
- [x] Update both kernel consumers and their structural synchronization test.
- [x] Pass the focused regression, workspace test/check, gated compilation,
      zero-warning Clippy, formatting, and repository policy gates.
- [ ] Pass Linux kernel evaluation/build in pull-request CI.
- [ ] Merge the issue-linked pull request and close #3248.

## Source verification

The upstream archive SHA-256 is
`8cee19e1839bb6ff4d5254d761933ae6ab670492d5ed030e09a80538320d5c4c`,
which converts to the Nix SRI hash
`sha256-jO4Z4YObtv9NUlTXYZM65qtnBJLV7QMOCagFODINXEw=`.

The downloaded archive matches kernel.org's clearsigned `v6.x/sha256sums.asc`
manifest. The detached signature over the uncompressed archive independently
verifies against release key fingerprint
`647F 2865 4894 E3BD 4571 99BE 38DB BDC8 6092 693E`.

## Rollout

This repository pin does not modify already-running VMs. Locally managed VMs
move with `mvmctl vm rekernel`; fleet rollout remains owned by mvmd.
