# Optional environment variables for OpenClaw gateway
# Mount this directory with: --config-dir ./config
# Sourced at runtime from /mnt/config/env.sh

# Instance name (used in OpenClaw's config via envsubst)
# export OPENCLAW_INSTANCE_NAME=my-openclaw

# Node.js memory limit (default: V8 auto-sizes based on available RAM)
# export NODE_OPTIONS="--max-old-space-size=2048"
