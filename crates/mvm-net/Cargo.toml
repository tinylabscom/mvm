[package]
name = "mvm-net"
version.workspace = true
edition.workspace = true
description = "NetworkProvider trait + provisioning/policy/registry seam for mvm. The TAP/bridge/native-gateway/passt impl lives in mvm-backend; the WireGuard/Tailscale mesh impl is mvmd's."
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true

[dependencies]
mvm-core.workspace = true
# IR `NetworkMode` the registry resolves. Acyclic: mvm-sdk does not
# depend on mvm-net; its only mvm-* normal dep is mvm-core.
mvm-sdk.workspace = true

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
