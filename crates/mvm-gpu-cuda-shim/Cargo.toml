[package]
name = "mvm-gpu-cuda-shim"
description = "Guest drop-in libcuda.so.1 shim: forwards the CUDA Driver API to the mvm host GPU endpoint over vsock"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true

[lib]
name = "cuda"
crate-type = ["cdylib"]

[dependencies]
mvm-gpu-shim-core = { path = "../mvm-gpu-shim-core" }
mvm-contract = { workspace = true }
libc.workspace = true

[lints]
workspace = true
