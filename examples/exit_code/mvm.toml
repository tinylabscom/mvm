# Manifest for the exit_code example.
# This is a one-shot sealed workload that exits with code 7, exercising
# the WS-A guest exit-report path.
flake = "."
profile = "default"
cpus = 1
mem = "256M"
data_disk = "0"
net = false
