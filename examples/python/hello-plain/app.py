"""Plain Python example with no SDK dependency.

This is the simplest possible Python workload: no `import mvm`, no lockfile,
no compile step. It is meant to be run directly inside a guest VM with a
Python OCI image.
"""

print("hello from mvm python")
